# Unconventional Features ML Pipeline - Requirements
# Install with: pip install -r requirements.txt
#
# NOTE: This pipeline will FAIL FAST if required dependencies are missing.
# Optional dependencies are clearly marked and the pipeline will skip
# corresponding features if not installed.

# =============================================================================
# Core Data Processing (REQUIRED)
# =============================================================================
polars>=0.20.0,<2.0.0
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<3.0.0
pyarrow>=14.0.0  # Required for pandas<->polars conversion

# =============================================================================
# Machine Learning (REQUIRED)
# =============================================================================
scikit-learn>=1.3.0,<2.0.0
xgboost>=2.0.0,<3.0.0
lightgbm>=4.0.0,<5.0.0

# =============================================================================
# Database (REQUIRED)
# =============================================================================
sqlalchemy>=2.0.0,<3.0.0
psycopg[binary]>=3.1.0,<4.0.0

# =============================================================================
# Technical Analysis (REQUIRED)
# =============================================================================
ta>=0.11.0,<1.0.0
scipy>=1.11.0,<2.0.0

# =============================================================================
# Utilities (REQUIRED)
# =============================================================================
tqdm>=4.65.0
requests>=2.31.0
python-dateutil>=2.8.0
pyyaml>=6.0.0  # For config file parsing

# =============================================================================
# Monitoring (OPTIONAL - enabled via ENABLE_PROMETHEUS=true)
# =============================================================================
prometheus-client>=0.19.0

# =============================================================================
# Optional: Astro Features
# Install with: pip install pyephem
# Enable via: ENABLE_ASTRO_FEATURES=true
# If not installed, astro features will be skipped gracefully
# =============================================================================
# pyephem>=4.1.0

# =============================================================================
# Optional: GPU/Image Features
# Install with: pip install opencv-python-headless torch torchvision
# Enable via: ENABLE_IMAGE_FEATURES=true
# Prefer opencv-python-headless for containers (no GUI dependencies)
# =============================================================================
# opencv-python-headless>=4.8.0
# torch>=2.0.0
# torchvision>=0.15.0

# =============================================================================
# Development Dependencies
# Install with: pip install -r requirements-dev.txt (if exists)
# Or uncomment below for development
# =============================================================================
# pytest>=7.4.0
# pytest-cov>=4.1.0
# black>=23.0.0
# ruff>=0.1.0
# mypy>=1.5.0
